version: '3.9'

services:
  vite-app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
        VITE_HOST: ${VITE_HOST}
    ports:
      - '80:80'
